# Copy to .env (or .env.<Firebase project id>) before the first Functions deploy
# for this project. Later deploys reuse these values. Required — there is no
# code fallback if this file is omitted.
#
# Must match NEXT_PUBLIC_FUNCTIONS_REGION on the matching Vercel project.
FUNCTIONS_REGION=australia-southeast1
SCHEDULE_TIMEZONE=Australia/Sydney
