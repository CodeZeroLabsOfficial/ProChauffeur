---
description: Nested branch data helpers
alwaysApply: false
globs: lib/branch/**/*
---

# Branch data layer

Use `branchCollection` / `branchSettingsDoc` path helpers. Reads and writes use
nested paths under `branches/{branchId}/` only — no dual-read or legacy
top-level ops fallbacks.
